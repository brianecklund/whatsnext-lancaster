import { NextRequest, NextResponse } from "next/server";
import { searchVenueSuggestions } from "@/lib/venue-import";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET(request: NextRequest) { try { const { searchParams } = new URL(request.url); const location = searchParams.get("location") || "Lancaster, PA"; const query = searchParams.get("query") || searchParams.get("q") || ""; const limit = Math.min(Number(searchParams.get("limit") || "8"), 10); const venues = query.trim() ? await searchVenueSuggestions(query, location, limit) : []; return NextResponse.json({ ok: true, source: "google-search", count: venues.length, venues }); } catch (error) { return NextResponse.json({ ok: false, error: error instanceof Error ? error.message : "Unknown error" }, { status: 500 }); } }
